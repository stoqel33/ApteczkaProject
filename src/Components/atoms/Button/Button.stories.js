import React from 'react';

import Button from './Button';

export default {
  component: Button,
  title: 'Button',
};

export const Primary = () => <Button>Dodaj</Button>;
export const Secondary = () => <Button>Usuń</Button>;
